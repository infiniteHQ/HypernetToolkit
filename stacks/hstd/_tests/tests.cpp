#include <catch2/catch_all.hpp>
#include <string>

#include "../../htk.h"
#include "../../htk_internals.h"

#define CATCH_CONFIG_MAIN

// Matrixes
TEST_CASE("Contructor", "[matrixes]"){
    // Create base matrix
    REQUIRE(true);
    // Destruct base matrix
    REQUIRE(true);

    // Create base matrix
    REQUIRE(true);
    // Destruct base matrix
    REQUIRE(true);

    // Create base matrix
    REQUIRE(true);
    // Destruct base matrix
    REQUIRE(true);

    // Create base matrix
    REQUIRE(true);
    // Destruct base matrix
    REQUIRE(true);

}

TEST_CASE("Initializator", "[context]"){
    REQUIRE(true);
}

